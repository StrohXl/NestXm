"use client";
import { createTheme } from "@mui/material/styles";
export const theme = createTheme({
  palette: {
    primary: {
      main: "#9c27b0",
      light: "#ba68c8",
      dark: "7b1fa2",
      contrastText: "#fff",
    },
  },
});
